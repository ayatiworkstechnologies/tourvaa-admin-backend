meta {
  name: Supplier Detail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/suppliers/{{supplierId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}
